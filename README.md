This in training project, which implements server to control smart home appliance, with NodeJs and MongoDB.

Run backend:
    `cd backend`
    `npm start`

Run frontend:
    `cd frontend`
    `npm start`

Run fake device on 3010 port:
    `cd backend`
    `node fakeDevice.js 3010`